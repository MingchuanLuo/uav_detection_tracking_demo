opencv-python>=4.10,<5

# Install the CUDA build from the official PyTorch index before this file when
# GPU training is required; see README.md.
torch>=2.7,<3
torchvision>=0.22,<1
ultralytics>=8.4.120,<9
lap>=0.5.12,<1
